"""{{ desc }}

Auto-generated by developer-factory. Edit the run() body, keep the schema.
"""

from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, Field


class {{ class_name }}Input(BaseModel):
{% if params %}
{% for p in params %}
    {{ p.name }}: {{ p.annotation }}{% if not p.required %} = {{ p.default }}{% endif %}
{% endfor %}
{% else %}
    payload: dict[str, Any] = Field(default_factory=dict)
{% endif %}


class {{ class_name }}Output(BaseModel):
    ok: bool
    data: dict[str, Any] = Field(default_factory=dict)
    error: str | None = None


def run(params: {{ class_name }}Input) -> {{ class_name }}Output:
    # Developer agent fills this in.
    return {{ class_name }}Output(ok=True, data=params.model_dump())


if __name__ == "__main__":
    import json
    import sys

    raw = json.loads(sys.stdin.read() or "{}")
    out = run({{ class_name }}Input.model_validate(raw))
    print(out.model_dump_json())
